# PostgreSQL primary – WAL-level replication config
listen_addresses = '*'
port = 5432

# WAL / Replication
wal_level = replica
max_wal_senders = 4
wal_keep_size = 64     # MB of WAL segments to retain

# Logging (handy for demo)
log_statement = 'mod'
log_replication_commands = on
